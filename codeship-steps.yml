- name: unittest
  service: unittest
  command: bash -c "npm test && ls -al && bash <(curl -s https://codecov.io/bash)"
# - name: checkdir
#   service: unittest
#   command: ls -al
# - name: codecov
#   service: unittest
#   command: bash -c "bash <(curl -s https://codecov.io/bash)"
